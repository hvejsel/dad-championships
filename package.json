{
  "name": "dad-championships",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Run a multi-sport championship across three generations",
  "scripts": {
    "start": "node server.mjs",
    "test": "node test.mjs && node test-sync.mjs && node test-push.mjs"
  },
  "dependencies": {
    "web-push": "^3.6.7"
  },
  "engines": { "node": ">=22" }
}
